{
  "buildCommand": "cd artifacts/seodxb && npm install && PORT=3000 BASE_PATH=/ npm run build",
  "outputDirectory": "artifacts/seodxb/dist/public",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
